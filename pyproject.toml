[project]
name = "edensummary"
version = "0.1.0"
description = "AI System for Summary long meetings"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "email-validator>=2.3.0",
    "fastapi~=0.136.1",
]
